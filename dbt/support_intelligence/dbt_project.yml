name: support_intelligence
version: 1.0.0
config-version: 2

profile: support_intelligence

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  support_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
